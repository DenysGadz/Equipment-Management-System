export interface Equipment {
  id?: string;
  name: string;
  inventoryNumber: string;
  category: string;
  location: string;
  responsiblePerson: string;
  condition: string;
  commissioningDate: string;
  note: string;
}